// engine.js is the original game code, kept as untyped JavaScript on purpose:
// typing it would mean rewriting it, and the point of the port is that it stays
// byte-for-byte the code that already works. It exports nothing — it runs for
// its side effects and wires itself to the DOM by element id.
export {};
